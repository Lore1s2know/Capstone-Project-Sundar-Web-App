location ~* ^/flux/(flux|editor)(\.min)?\.(js|css)$ {
    expires off;
    try_files $uri $uri/ /index.php?$query_string;
}

location ~* ^/livewire {
    try_files $uri $uri/ /index.php?$query_string;
}
